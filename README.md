# azdeeploy

`azdeeploy` is a Python 3.11+ CLI project scaffold for deployment diagnostics,
Azure helpers, project scanning, and LLM-assisted analysis.

## Install

```bash
pip install -e .
```

## Run

```bash
azdeeploy --help
```
